---
import BaseLayout from '../layouts/BaseLayout.astro';
import Newsletter from '../components/sections/Newsletter.astro';
import { Image } from 'astro:assets';

// Diseño provisional mientras no haya fotos propias de la nave ni el vídeo del
// dron: todo son imágenes de alta resolución que no se usan en ninguna otra
// página (comprobado contra dist/ por huella perceptual el 2026-09-21).
import imgHero from '../assets/proyectos/floor-1.jpg';
import imgBanda from '../assets/proyectos/wall-2.jpg';
import imgObra1 from '../assets/proyectos/wall-1.jpg';
import imgObra2 from '../assets/proyectos/arcilla-1.jpg';
import imgEquipo from '../assets/formacion/aplicador.png';
import imgEncimera from '../assets/proyectos/floor-2.jpg';
import imgCal from '../assets/nosotros/pasta-cal.png';
import imgCorcho from '../assets/nosotros/corcho-macro.jpg';

const origen = {
  num: '01',
  kicker: 'Origen',
  title: 'Donde nace cada producto',
  text: 'Más de 30 años fabricando en España. Bienvenido a MTM — Materiales Modernos.',
};

const pilares = [
  { num: '02', kicker: 'Fabricación',        title: 'Materiales que resuelven', text: 'Pinturas aislantes, morteros de cal natural, epoxi 3D y revestimientos de fibras naturales, desarrollados aquí con obsesión por el detalle.', img: imgEncimera, alt: 'Encimera con acabado continuo de MTM' },
  { num: '03', kicker: 'Innovación',         title: 'Tecnología y tradición',   text: 'Combinamos la tecnología más innovadora con materiales nobles como la cal, para acabados sanos, transpirables y atemporales.', img: imgCal, alt: 'Primer plano de pasta de cal madura' },
  { num: '04', kicker: 'Control de calidad', title: 'Nada sale sin revisarse',  text: 'Cada lote pasa un control exhaustivo para garantizar el mismo resultado, una y otra vez.', img: imgCorcho, alt: 'Detalle macro de un revestimiento de corcho' },
];

const equipo = {
  num: '05',
  kicker: 'El equipo',
  title: 'Una empresa familiar',
  text: 'Detrás de cada bote hay un equipo que cuida lo que hace. Ese es el compromiso de MTM.',
};

const stats = [
  { n: '+30', label: 'Años de experiencia' },
  { n: '3', label: 'Marcas propias y distribuidas' },
  { n: '+200', label: 'Productos en catálogo' },
  { n: '100%', label: 'Fabricación española' },
];
---

<BaseLayout
  title="Sobre MTM · Nosotros"
  description="MTM es una empresa familiar dedicada a la fabricación de pinturas y revestimientos de alta calidad con la tecnología más innovadora. Conoce cómo trabajamos y el equipo que hay detrás."
  darkHeader
>
  <!-- ===== Intro: texto + imagen ===== -->
  <section class="nos-intro">
    <div class="intro-text">
      <span class="label label--tick nos-eyebrow">Sobre MTM</span>
      <h1 class="nos-title">Fabricamos <span class="serif-italic">con intención</span></h1>
      <p class="nos-lead">
        Una empresa familiar dedicada a la fabricación de pinturas de alta calidad con la
        tecnología más innovadora. Baja y conócenos.
      </p>
      <span class="nos-scroll" aria-hidden="true">
        Desliza para conocernos
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4"><path d="M12 5v14M6 13l6 6 6-6"/></svg>
      </span>
    </div>
    <figure class="intro-fig">
      <Image
        src={imgHero}
        alt="Muro curvo revestido con cal junto a una piscina"
        widths={[700, 1100, 1600, 2200]}
        sizes="(max-width: 880px) 100vw, 46vw"
        quality={86}
        loading="eager"
        class="intro-img"
      />
    </figure>
  </section>

  <!-- ===== Origen ===== -->
  <section class="nband nband--tall">
    <div class="nband-media">
      <Image
        src={imgBanda}
        alt="Interior de restaurante con paredes texturizadas en tonos rosados"
        widths={[1200, 1800, 2560]}
        sizes="100vw"
        quality={86}
        class="nband-img"
      />
      <div class="nband-scrim"></div>
    </div>
    <div class="nband-inner container-mtm">
      <div class="nband-content reveal">
        <span class="nband-eyebrow">{origen.num} · {origen.kicker}</span>
        <h2 class="nband-title">{origen.title}</h2>
        <p class="nband-text">{origen.text}</p>
      </div>
    </div>
  </section>

  <!-- ===== Pilares ===== -->
  <section class="nos-pilares">
    <div class="container-mtm">
      <div class="pilares-grid">
        {pilares.map((p) => (
          <article class="pilar reveal">
            <figure class="pilar-fig">
              <Image src={p.img} alt={p.alt} widths={[480, 800, 1100]} sizes="(max-width: 880px) 100vw, 31vw" quality={84} class="pilar-img" />
            </figure>
            <span class="pilar-num">{p.num}</span>
            <span class="pilar-kicker">{p.kicker}</span>
            <h3 class="pilar-title">{p.title}</h3>
            <p class="pilar-text">{p.text}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== Obra ===== -->
  <section class="nos-obra">
    <div class="container-mtm">
      <div class="obra-head reveal">
        <span class="nband-eyebrow">En obra</span>
        <h2 class="obra-title">Acabados que <span class="serif-italic">se viven</span></h2>
      </div>
      <div class="obra-grid">
        <figure class="obra-fig obra-fig--wide reveal">
          <Image src={imgObra1} alt="Jardinera curva revestida con cal e iluminación integrada" widths={[800, 1300, 1900]} sizes="(max-width: 880px) 100vw, 60vw" quality={86} class="obra-img" />
        </figure>
        <figure class="obra-fig obra-fig--tall reveal">
          <Image src={imgObra2} alt="Estancia revestida con arcilla en tonos terracota" widths={[600, 1000, 1400]} sizes="(max-width: 880px) 100vw, 36vw" quality={86} class="obra-img" />
        </figure>
      </div>
    </div>
  </section>

  <!-- ===== El equipo ===== -->
  <section class="nband nband--right">
    <div class="nband-media">
      <Image
        src={imgEquipo}
        alt="Aplicadores trabajando con productos de MTM durante una formación"
        widths={[1200, 1800, 2600]}
        sizes="100vw"
        quality={86}
        class="nband-img"
      />
      <div class="nband-scrim"></div>
    </div>
    <div class="nband-inner container-mtm">
      <div class="nband-content reveal">
        <span class="nband-eyebrow">{equipo.num} · {equipo.kicker}</span>
        <h2 class="nband-title">{equipo.title}</h2>
        <p class="nband-text">{equipo.text}</p>
      </div>
    </div>
  </section>

  <!-- ===== Datos ===== -->
  <section class="nos-stats">
    <div class="container-mtm stats-grid">
      {stats.map((s) => (
        <div class="stat reveal">
          <span class="stat-n">{s.n}</span>
          <span class="stat-l">{s.label}</span>
        </div>
      ))}
    </div>
  </section>

  <!-- ===== Cita ===== -->
  <section class="nos-quote">
    <div class="container-mtm">
      <blockquote class="quote reveal">
        <p class="quote-mark serif-italic">“</p>
        <p class="quote-text">
          Nuestra apuesta por la innovación es innegociable. Trabajamos día a día para
          desarrollar productos que den solución a problemas específicos del sector de la
          pintura, reformas y decoración.
        </p>
        <footer class="quote-by">
          <span class="quote-name">Rubén Sabater</span>
          <span class="quote-role">CEO · MTM Materiales Modernos</span>
        </footer>
      </blockquote>
    </div>
  </section>

  <Newsletter />
</BaseLayout>

<style>
  /* La página entera en oscuro */
  :global(body:has(.nos-intro)) { background: var(--color-carbon); }

  /* ===== Intro ===== */
  .nos-intro {
    background: var(--color-carbon);
    color: var(--color-paper);
    min-height: 92vh;
    display: grid; grid-template-columns: 1.05fr 1fr; align-items: center;
    gap: clamp(2rem, 5vw, 5rem);
    padding: clamp(6rem, 12vw, 9rem) clamp(1.25rem, 5vw, 5rem) clamp(3rem, 6vw, 5rem);
    max-width: 92rem; margin-inline: auto;
  }
  .intro-text { display: flex; flex-direction: column; }
  .nos-eyebrow { color: rgba(244,244,242,0.7); margin-bottom: 1.6rem; }
  .nos-title {
    color: var(--color-paper);
    font-size: clamp(2.8rem, 7vw, 6.4rem); line-height: 0.98; letter-spacing: -0.01em;
    margin-bottom: 1.8rem; max-width: 16ch;
  }
  .nos-lead { color: rgba(244,244,242,0.78); font-size: clamp(1.1rem, 1.6vw, 1.35rem); line-height: 1.6; max-width: 44ch; }
  .nos-scroll {
    margin-top: clamp(2.5rem, 6vw, 4rem);
    display: inline-flex; align-items: center; gap: 0.7rem;
    font-size: 0.8rem; letter-spacing: 0.18em; text-transform: uppercase; color: rgba(244,244,242,0.6);
  }
  .nos-scroll svg { width: 18px; height: 18px; animation: bob 1.8s var(--ease-soft) infinite; }
  @keyframes bob { 0%,100%{ transform: translateY(0); } 50%{ transform: translateY(5px); } }
  .intro-fig { margin: 0; height: min(74vh, 760px); overflow: hidden; border-radius: 2px; }
  .intro-img { width: 100%; height: 100%; object-fit: cover; }

  /* ===== Bandas a sangre ===== */
  .nband {
    position: relative;
    min-height: clamp(440px, 68vh, 700px);
    display: flex; align-items: flex-end;
    padding-block: clamp(2.5rem, 7vh, 5rem);
    overflow: hidden;
    background: var(--color-carbon);
  }
  .nband--tall { min-height: clamp(520px, 82vh, 860px); }
  .nband-media { position: absolute; inset: 0; }
  .nband-img { width: 100%; height: 100%; object-fit: cover; transition: transform 1.4s var(--ease-soft); }
  .nband:hover .nband-img { transform: scale(1.04); }
  /* Doble degradado: desde abajo para el texto, lateral para separarlo de la foto */
  .nband-scrim {
    position: absolute; inset: 0;
    background:
      linear-gradient(to top, rgba(11,11,10,0.94) 0%, rgba(11,11,10,0.6) 34%, rgba(11,11,10,0.1) 68%, rgba(11,11,10,0) 100%),
      linear-gradient(to right, rgba(11,11,10,0.9) 0%, rgba(11,11,10,0.58) 30%, rgba(11,11,10,0.14) 62%, rgba(11,11,10,0) 86%);
  }
  .nband--right .nband-scrim {
    background:
      linear-gradient(to top, rgba(11,11,10,0.94) 0%, rgba(11,11,10,0.6) 34%, rgba(11,11,10,0.1) 68%, rgba(11,11,10,0) 100%),
      linear-gradient(to left, rgba(11,11,10,0.9) 0%, rgba(11,11,10,0.58) 30%, rgba(11,11,10,0.14) 62%, rgba(11,11,10,0) 86%);
  }
  .nband-inner { position: relative; z-index: 2; width: 100%; }
  .nband-content { max-width: 48ch; color: var(--color-paper); }
  .nband--right .nband-content { margin-left: auto; text-align: right; }
  .nband-eyebrow {
    display: inline-block;
    font-size: 0.8rem; letter-spacing: 0.22em; text-transform: uppercase;
    color: rgba(244,244,242,0.75); margin-bottom: 1.1rem;
  }
  .nband-title {
    color: var(--color-paper);
    font-size: clamp(2rem, 5vw, 3.8rem); line-height: 1.02;
    letter-spacing: -0.01em; margin-bottom: 1.1rem;
  }
  .nband-text { color: rgba(244,244,242,0.88); font-size: clamp(1.05rem, 1.5vw, 1.25rem); line-height: 1.6; }

  /* ===== Pilares ===== */
  .nos-pilares {
    background: var(--color-carbon); color: var(--color-paper);
    padding-block: clamp(3.5rem, 8vw, 6.5rem);
  }
  .pilares-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: clamp(2rem, 3.5vw, 3rem); }
  .pilar { display: flex; flex-direction: column; }
  .pilar-fig { margin: 0 0 1.6rem; aspect-ratio: 4 / 3; overflow: hidden; border-radius: 2px; }
  .pilar-img { width: 100%; height: 100%; object-fit: cover; transition: transform 1.2s var(--ease-soft); }
  .pilar:hover .pilar-img { transform: scale(1.05); }
  .pilar-num {
    display: block; font-family: var(--font-display);
    font-size: clamp(1.6rem, 2.6vw, 2.2rem); line-height: 1;
    color: rgba(244,244,242,0.35); margin-bottom: 0.8rem;
  }
  .pilar-kicker {
    display: block; font-size: 0.78rem; letter-spacing: 0.2em;
    text-transform: uppercase; color: rgba(244,244,242,0.62); margin-bottom: 0.7rem;
  }
  .pilar-title { color: var(--color-paper); font-size: clamp(1.35rem, 2.2vw, 1.9rem); line-height: 1.12; margin-bottom: 0.8rem; }
  .pilar-text { color: rgba(244,244,242,0.8); font-size: 1rem; line-height: 1.6; }

  /* ===== Obra ===== */
  .nos-obra {
    background: var(--color-carbon); color: var(--color-paper);
    padding-block: clamp(1rem, 3vw, 2rem) clamp(4rem, 9vw, 7rem);
  }
  .obra-head { margin-bottom: clamp(2rem, 4vw, 3rem); }
  .obra-title { color: var(--color-paper); font-size: clamp(2rem, 4.6vw, 3.6rem); line-height: 1.02; letter-spacing: -0.01em; }
  .obra-grid { display: grid; grid-template-columns: 1.65fr 1fr; gap: clamp(1rem, 2vw, 1.6rem); align-items: end; }
  .obra-fig { margin: 0; overflow: hidden; border-radius: 2px; }
  .obra-fig--wide { aspect-ratio: 3 / 2; }
  .obra-fig--tall { aspect-ratio: 4 / 5; }
  .obra-img { width: 100%; height: 100%; object-fit: cover; transition: transform 1.2s var(--ease-soft); }
  .obra-fig:hover .obra-img { transform: scale(1.04); }
  /* La sala de arcilla tiene una pantalla con texto a la izquierda: se encuadra a la derecha */
  .obra-fig--tall .obra-img { object-position: 78% center; }

  /* ===== Datos ===== */
  .nos-stats { background: var(--color-carbon); color: var(--color-paper); padding-block: clamp(3.5rem, 8vw, 6rem); border-top: 1px solid rgba(244,244,242,0.1); }
  .stats-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2rem; }
  .stat { display: flex; flex-direction: column; gap: 0.5rem; }
  .stat-n { color: var(--color-paper); font-family: var(--font-display); font-size: clamp(2.6rem, 5vw, 4rem); line-height: 1; letter-spacing: -0.01em; }
  .stat-l { font-size: 0.897rem; letter-spacing: 0.1em; text-transform: uppercase; color: rgba(244,244,242,0.6); }

  /* ===== Cita ===== */
  .nos-quote { background: var(--color-carbon); color: var(--color-paper); padding-block: clamp(3rem, 7vw, 6rem) clamp(4rem, 9vw, 7rem); }
  .quote { margin: 0; max-width: 60rem; }
  .quote-mark { font-size: 4rem; line-height: 0.4; color: rgba(244,244,242,0.4); display: block; }
  .quote-text { color: var(--color-paper); font-family: var(--font-serif); font-style: italic; font-size: clamp(1.6rem, 3.4vw, 2.6rem); line-height: 1.3; margin: 1.4rem 0 2rem; }
  .quote-by { display: flex; flex-direction: column; gap: 0.2rem; }
  .quote-name { color: var(--color-paper); font-size: 1.15rem; }
  .quote-role { font-size: 0.851rem; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(244,244,242,0.6); }

  /* ===== Responsive ===== */
  @media (max-width: 880px) {
    .nos-intro { grid-template-columns: 1fr; min-height: 0; }
    .intro-fig { height: auto; aspect-ratio: 4 / 5; }
    .pilares-grid { grid-template-columns: 1fr; gap: 3rem; }
    .pilar-fig { aspect-ratio: 3 / 2; }
    .obra-grid { grid-template-columns: 1fr; }
    /* En movil el texto ocupa el ancho: el degradado pasa a ser solo vertical */
    .nband-content { max-width: none; }
    .nband--right .nband-content { margin-left: 0; text-align: left; }
    .nband-scrim,
    .nband--right .nband-scrim {
      background: linear-gradient(to top, rgba(11,11,10,0.95) 0%, rgba(11,11,10,0.65) 40%, rgba(11,11,10,0.15) 75%, rgba(11,11,10,0) 100%);
    }
    .stats-grid { grid-template-columns: repeat(2, 1fr); gap: 2rem 1rem; }
  }

  @media (prefers-reduced-motion: reduce) {
    .nos-scroll svg { animation: none; }
    .nband-img, .pilar-img, .obra-img { transition: none; transform: none !important; }
  }
</style>
